module.exports = {
    singleQuote: true, // 字符串使用单引号
    printWidth: 180, // 换行字符长度
    semi: true, // 所有末尾加分号
    trailingComma: 'all', // 所有结尾都有尾,号
    jsxBracketSameLine: false, // jsx > 在代码最后，单独起一行
    arrowParens: 'always', // 箭头函数只有一个参数也需要使用()号
    jsxSingleQuote: true,
    proseWrap: 'never',
    tabWidth: 4,
};
